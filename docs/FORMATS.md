# Supported Formats & Technical Reference

This document contains detailed technical notes, reverse-engineering findings, and the complete format capability registry for **Wiimms SZS Tools Plus**.

---

## Format Capability Index

| Format | Platform / Category | Decode | Encode | Notes |
|---|---|---|---|---|
| **AJJPG / AJPG** | GBA / Still Image | ✅ | ✅ | GBA-era still image container |
| **ADJB** | Switch / Model sidecar | ✅ | ❌ | Smash Ultimate mesh triangle adjacency (model.adjb): per-mesh id + u16 index lists, sized like Adjb.cs |
| **ALAR** | DS / Archive | ✅ | ✅ | Nitro ALAR archive |
| **ALZ1** | DS / Compression | ✅ | ✅ | Arika 4096-byte window LZSS with inverted flag bits |
| **Arika (INFO.DAT/GAME.DAT)** | DS/DSi / Archive | ✅ | ✅ | Obfuscated directory decryption and member decompression |
| **ARCV** | Wii / Archive | ✅ | ✅ | Pac-Man Party (Wii) archive; byte-exact round-trip |
| **ART / IMG** | Wii / Texture | ✅ | ✅ | Monster Games GUI image format |
| **ASH0** | GameCube/Wii / Compression | ✅ | ✅ | Nintendo ASH0 compression (System Menu, Animal Crossing, My Pokémon Ranch; automatic 11/15-bit distance tree fallback) |
| **AGI** | Wii / Archive | ✅ | ❌ | Toys for Bob "AGI" archive (`\x1AAGI`, *Skylanders: Swap Force* `files/*.pak`): big-endian header + backslash-path name table; extract-only. Two entry-table record layouts, tried in order: plain 2-word `(offset,size)` pairs (pure audio-bank `.pak` files) and 4-word `(0, offset, unk, flag)` groups whose on-disk size is derived from the gap to the next block-aligned offset (mixed audio + `.igz`/`.igx` model/material `.pak` files). Verified against all 2339 retail `.pak` files sampled from the Swap Force DATA partition: 100% parse successfully (1559 via the 2-word layout, 780 via the 4-word layout). Raw `FSB5` audio members are tagged `.fsb` and auto-decoded to WAV via the existing FSB4/FSB5 decoder; `.igz`/`.igx` model/material members are extracted raw (IGZ internals out of scope) |
| **AT7** | PS2/Wii / Archive | ✅ | ✅ | Koei Tecmo container |
| **BCFNT** | 3DS / Font | ✅ | ✅ | 3DS bitmap font to PNG atlas |
| **BCH** | 3DS / Model | ✅ | ✅ | CTR H3D model container |
| **BCLAN** | 3DS / Layout | ✅ | ✅ | 3DS layout animation |
| **BCLIM** | 3DS / Texture | ✅ | ✅ | CTR image container |
| **BCLYT** | 3DS / Layout | ✅ | ✅ | 3DS binary layout |
| **BCRES** | 3DS / Model | ✅ | ✅ | CTR graphics container |
| **BCSAR** | 3DS / Audio Archive | ✅ | ✅ | CTR Sound Archive (CSAR) |
| **BCWAV** | 3DS / Audio | ✅ | ✅ | CTR Sound Wave |
| **BCWAR** | 3DS / Audio Archive | ✅ | ✅ | CTR Sound Wave Archive (CWAR) |
| **BCGRP** | 3DS / Audio Archive | ✅ | ✅ | CTR Sound Group Archive (CGRP) |
| **BFFNT** | Wii U / Font | ✅ | ✅ | Wii U bitmap font to PNG atlas |
| **BG4** | 3DS / Archive | ✅ | ✅ | Mario & Luigi flat archive with BLZ member compression |
| **BIGF** | Wii / Archive | ✅ | 🟡 | EA BIGF container (encode round-trips losslessly; not a byte-exact retail reproduction — no retail `.big` sample to derive member ordering/padding from) |
| **BFLAN** | Wii U / Layout | ✅ | ✅ | Wii U layout animation |
| **BFLIM** | Wii U / Texture | ✅ | ✅ | Wii U textures (BC1-BC5) |
| **BFLYT** | Wii U / Layout | ✅ | ✅ | Wii U binary layout |
| **BFRES** | Wii U / Switch / Model | ✅ | ✅ | GX2/NX model container (passthrough / extraction) |
| **BFSAR** | Wii U / Switch / Audio Archive | ✅ | ✅ | Sound Archive (FSAR) |
| **BFWAV** | Wii U / Switch / Audio | ✅ | ✅ | Sound Wave |
| **BFWAR** | Wii U / Switch / Audio Archive | ✅ | ✅ | Sound Wave Archive (FWAR) |
| **BFGRP** | Wii U / Switch / Audio Archive | ✅ | ✅ | Sound Group Archive (FGRP) |
| **BLZ** | DS / Compression | ✅ | ✅ | Nitro backward-LZSS |
| **BPE / GFCP** | Wii / Compression | ✅ | ✅ | Good-Feel Byte Pair Encoding (GFAC mode 1) |
| **BMD** | DS / Model | ✅ | ✅ | Early Nitro 3D models |
| **J3DBMD** | GameCube / Wii / Model | ✅ | ✅ | J3D binary model (`J3D2bmd3`, legacy `bmd2`); SuperBMD-compatible GLB round-trip (geometry, skinning, materials, GX textures incl. mipmaps; canonical single-TEV encode, RGBA32/CMPR, triangle lists) + material/texheader JSON sidecars + `--profile` |
| **J3DBDL** | GameCube / Wii / Model | ✅ | ✅ | J3D binary display list (`J3D2bdl4`); same coverage as J3DBMD, MDL3 section written as a parseable stub (prefer BMD for in-game use) |
| **BNTX** | Switch / Texture | ✅ | ✅ | Switch texture container (Tegra block-linear); native DDS/ASTC block export |
| **BREFT** | Wii / Texture | ✅ | ✅ | Brawl effect texture |
| **BRFNA / BRFNT** | Wii / Font | ✅ | ✅ | NW4R font to PNG atlas + XML metrics |
| **BRLAN / BRLYT** | Wii / Layout | ✅ | ✅ | NW4R layout and animations |
| **BRRES (MDL0, TEX0)** | Wii / Graphics | ✅ | ✅ | NW4R models, textures, animations |
| **BRSAR** | Wii / Audio Archive | ✅ | ✅ | NW4R sound archive |
| **BRSTM / BFSTM / BCSTM** | Wii/Wii U/3DS / Audio Stream | ✅ | ✅ | Multichannel ADPCM/PCM streams |
| **BYAML / BYML** | Wii U / Switch / Data | ✅ | ✅ | Binary YAML format (versions 1–7, big- and little-endian, string/path/hash tables incl. reloc maps); path tables ride any version, not just v1; Yaz0-wrapped BotW containers (`.sbyml`, `.smubin`, …) decode transparently and re-compress on CREATE; encoder honors `.be`/`.vN` dest markers and `# byml version=N endian=..` headers; `wszst BYMLFIND` searches keys/values |
| **CA01 / SA01** | 3DS / Wii U / Archive | ✅ | ✅ | Mii Maker & amiibo settings flat archives |
| **CCF** | Wii / Switch / Archive | ✅ | ✅ | Virtual Console container |
| **CGFX** | 3DS / Model | ✅ | ✅ | CTR NW4C model container |
| **CRAM (.arc)** | 3DS / Archive | ✅ | ✅ | Xenoblade Chronicles 3D archive |
| **CSB** | Switch/Wii U / Collision | ✅ | ✅ | Paper Mario collision scene (TTYD/Origami King LE, Color Splash BE): triangle meshes with material/collision flags + sphere/box trigger volumes, GLB round-trip with generated `.ctb` |
| **CS_DCT** | Wii / Archive + Graphics | ✅ | ❌ | *Chicken Shoot* (Wii) stage archive (`.dct`): background planes (LZ-compressed indexed pixels + RGB555 palette) → PNG, animated sprites with per-frame bounding geometry → PNG, object placement metadata → `manifest.json` and raw binaries |
| **CTB** | Switch/Wii U / Collision | ✅ | ❌ | Paper Mario collision search table (XZ-quadtree over `.csb` triangles; regenerated on encode, inspected with `wmdlt CAT`) |
| **CTPK** | 3DS / Texture | ✅ | ✅ | CTR texture container |
| **DARC** | 3DS / Archive | ✅ | ✅ | Differential archive container |
| **DSB (TXTR)** | DS / Texture | ✅ | ✅ | Animal Crossing: Wild World menu texture (RGB555 palette + A3I5 texels); retail regression covers decode → encode → decode pixels |
| **DSP** | GameCube / Wii / Audio | ✅ | ❌ | Nintendo GameCube/Wii DSP-ADPCM standalone audio stream (`.dsp`): mono/stereo ADPCM coefficients, nibble predictor/scale decoding → 16-bit PCM WAV |
| **Retro TXTR** | Wii / Texture | ✅ | ✅ | Retro Studios texture (*Metroid Prime 1-3*, *DKCR*): GX-tiled, indexed + direct |
| **Tropical TXTR** | Wii U / Texture | ✅ | ❌ | Retro Studios texture (*Tropical Freeze*): RFRM form, GX2 detile |
| **DAT (Star Fox Zero)** | Wii U / Archive | ✅ | 🟡 | Big-endian flat archive |
| **Deflate** | Compression | ✅ | ✅ | Standard Deflate / Zlib streams |
| **DTLS (dt00/ls00)** | Wii U / 3DS / Archive | ✅ | ✅ | Super Smash Bros. 4 composite resource package & lookup (Wii U `of02` 16-byte + 3DS `of01` 12-byte retail variants included) |
| **FSYS** | GameCube / Archive | ✅ | ✅ | Genius Sonority Pokémon archive |
| **FZIP** | Wii U / Compression | ✅ | ✅ | Game & Wario Zlib container |
| **GFA** | 3DS / Archive | ✅ | ✅ | GFAC archive |
| **GF1MOT** | 3DS / Animation | ✅ | ❌ | Game Freak XY/ORAS bone-motion pack (SPICA GF1MotionPack): skeleton + per-anim frame/octet listing as text |
| **GFLX** | Switch / Archive | ✅ | ❌ | Game Freak GFLXPack archive (SPICA GFLXPack): raw-LZ4 members, magic-sniffed extensions |
| **GFMOT** | 3DS / Animation | ✅ | ❌ | Game Freak skeletal/material/visibility motion (SPICA GFMotion): frames, tracks and constants as text |
| **GFMPACK** | 3DS / Archive | ✅ | ❌ | Game Freak model/texture/shader pack (SPICA GFModelPack): named members with sniffed extensions |
| **GFMODEL** | 3DS / Model | ✅ | ❌ | Game Freak GFL2 3D model (SPICA GFModel, Pokémon X/Y/ORAS): skeleton, materials with texture names, PICA200 command-buffer geometry with smooth skinning → GLB |
| **GFPKG** | 3DS / Archive | ✅ | ❌ | Game Freak Gen6/Gen7 package (SPICA GFPackage): offset-table members with sniffed extensions |
| **GFTEX** | 3DS / Texture | ✅ | ❌ | Game Freak GFL2 texture (SPICA GFTexture): PICA200 payloads incl. ETC1 → PNG |
| **MBN** | 3DS / Model | ✅ | ❌ | ModelBinary companion buffers (SPICA MBn): replacement vertex/index buffers applied onto the sibling `.bch` base scene → GLB; descriptor listing as text standalone |
| **MTMFX** | 3DS / Shader | ✅ | ❌ | Capcom MT Framework Mobile shader effects (SPICA MTShaderEffects): input-layout table (vertex attribute names/formats/offsets) as text; layouts drive `wmdlt` MOD decoding |
| **MTMOD** | 3DS / Model | ✅ | ❌ | Capcom MT Framework Mobile model (SPICA MTModel): skeleton always; vertices via sibling `.mfx`/`.lfx` input layouts (else validated float fallback); materials/textures via sibling `.mrl` → GLB |
| **MTMRL** | 3DS / Material | ✅ | ❌ | Capcom MT Framework Mobile materials (SPICA MTMaterials): CRC32-keyed texture bindings as text |
| **MTTEX** | 3DS / Texture | ✅ | ❌ | Capcom MT Framework Mobile texture (SPICA MTTexture): PICA200 payloads incl. ETC1 → PNG |
| **GTX** | Wii U / Texture | ✅ | ✅ | Wii U GX2 texture container |
| **GSH** | Wii U / Shader | ✅ | ✅ | Wii U Latte GPU shader container |
| **HSD (.dat)** | GameCube / Model | ✅ | ✅ | HAL Laboratory sysdolphin object graph; also archive bundles (many archives in one file, `scene_data` roots, e.g. Doraemon `map*_dat.mdl`) merged into one GLB |
| **HSF** | GameCube / Wii / Model | ✅ | ✅ | Hudson Mario Party 3D model |
| **IPK** | Wii / Wii U / Switch / Archive | ✅ | ❌ | Ubisoft UbiArt archive (*Just Dance*, *Rayman Origins/Legends*): stored + zlib/LZMA members, old/new path-name orders |
| **WADH** | Wii / Archive | ✅ | ❌ | Data Design Interactive `DataWII.wad` (*Ninjabread Man*): stored members, directory tree via last-child / previous-sibling links |
| **DC2 DCX / DCT** | Wii / Archive + Texture | ✅ | ❌ | *Jakers! Kart Racing*: `.dcx` directory archives (backslash paths, contiguous members); `.dct` GX CMPR / RGBA8 textures (top mip) → PNG |
| **NIF (Gamebryo 20.6, Wii)** | Wii / Model + Texture | ✅ | ❌ | *Pocoyo Racing*: big-endian `.nif` scene graph; `NiMesh` GX display-list / INDEX streams → GLB in world space (skinned meshes in bind pose, hidden collision meshes only when nothing else exists); embedded DXT1/RGBA `NiPersistentSrcTextureRendererData` → PNG |
| **Grip RES** | Wii / Archive + Texture | ✅ | ❌ | *Sesame Street: Elmo's Musical Monsterpiece*: `res\n` serialised resource packages split into per-section files (`strings.txt`, `index.txt`, `.fsb` sound banks, compiled Lua, raw object sections); `surf` textures (CMPR / RGB5A3 / I8, bottom-up rows) → PNG. Model object graphs (`body`/`bmsh`/`gshd`) are pointer-linked and stay as raw sections |
| **Goliath GS Package** | Wii / Archive | ✅ | ❌ | *Skylanders: SuperChargers Racing* `files/Data/*.pkz`: a self-checking big-endian chunk tree (uniform 16-byte `id/version/has_children/size_hi/size` header, `0x80000001` root, every level consumes its parent's payload exactly). Named resources are `0x8000138d` wrappers whose first child (`0x8000138e`) carries a hash + NUL-terminated name; bulk texture (`0x80000195`) and audio (`0x80001134`) payloads are pooled elsewhere in the tree and matched to their `0x80000197`/`0x80001133` descriptions positionally, in document order (exact across all 782 retail files). Textures are GameCube-native mip chains (CMPR, RGB5A3, or CMPR+I8-alpha) re-wrapped as one-image TPLs → PNG; format 5 and mismatched-size RGB5A3 headers are declined rather than guessed. Audio is RIFX WAVE with wFormatTag 2 carrying a Nintendo DSP-ADPCM extension (not MS-ADPCM despite the tag): per-channel coefficient/gain/history state is re-packed as standalone `.dsp` streams and decoded through the existing DSP-ADPCM path (stereo → two mono `.dsp`, de-interleaved). Extract-only; geometry/materials/scripts/engine runtime tables are not interpreted. Verified against all 782 retail packages: 782/782 walk cleanly (586496 chunks, 216 distinct (id,version,children) combinations), 760/782 yield extractable content, 7031/7055 textures decode (99.66%, 19 format-5 + 5 size-mismatched RGB5A3 declined), 4994/4994 audio streams decode, 73900 names recovered |
| **CDGaCube CAR** | Wii / Archive | ✅ | ❌ | Cat Daddy Games `birthday.CAR` (*Birthday Party Bash*): 24-byte records, 2048-byte sector offsets, trailing path table; members stored raw or as bare zlib streams (inflated on extract) |
| **Terminal Reality POD** | Wii / Archive | ✅ | ❌ | POD3 / POD4 / POD5 (*Nickelodeon Dance* `WII*.POD`): stored members with backslash paths (converted to `/`); compressed POD4 members are skipped. Layout after jopadan/termpod |
| **Heavy Iron HO** | Wii / Archive | ✅ | ❌ | Good Engine `.ho` packages (*WALL-E*, *Up*, also *Ratatouille* on other platforms): big-endian MAST / SECT / PSL / PSLD tables; assets written as `name.<type hash>` with debug names from PSLD; GX texture blobs (RGB5A3 / RGBA8 / CMPR) → PNG. The `.lo` text log beside each package lists asset type names |
| **Asobo BigFile** | Wii / Archive | ✅ | ❌ | Asobo Studio "Internal Cross Technology" `.DRV` volumes (*Ratatouille*): big-endian v1.06.63 blocks, stored or LZRS members, hashed names (Asobo CRC) with class names (`Bitmap_Z`, `Mesh_Z`, ...); `Bitmap_Z` (RGB565 / RGBA8 / CMPR) → PNG; `Sound_Z` (Nintendo DSP-ADPCM with a standard DSP header) → WAV; `Mesh_Z` (Wii GX display lists over s16 positions / s16 UVs / s8 normals) → GLB with one primitive per material and the material's first bitmap as texture (static bind pose; `Skin_Z` skinning and `Node_Z` transforms are not applied). Other classes stay raw. Format after widberg/bff |
| **Bj engine** | Wii / Assets | ✅ | ❌ | *Super Karts* (Pro Kart): little-endian `.tx1` headers + `.tx2` 8-bit paletted pixels (2 KiB-unit chunks, square mip chains) to PNG; `.mtm` mesh trees (node hierarchy, 4x4 matrices, indexed list/strip/fan primitives, UVs) to textured GLB; `.bsi` big-endian sound banks (mono PCM8) and `.bsm` planar-stereo PCM8 music at 32 kHz to WAV. Layout recovered from the disc's symbolised ELF. |
| **Torus Games hunkfile** | Wii / Assets | ✅ | ❌ | *Barbie & Her Sisters: Puppy Rescue* `.hnk`: little-endian chunks `{u32 size, u16 kind, u16 0}` (low 12 bits of kind = record type, upper bits = memory pool) ending exactly at EOF; `0x71` names each asset (class + name), `0x150`/`0x151` = `TSETexture` header (big-endian width/height/mips) + GX mip chain (CMPR / RGBA8 / I8, told apart by chain size) to PNG; `0x92`/`0x93` = `SqueakStream` (`IWAR` header, big-endian samples/rate, DSP coefficients at +64 / +112) naming a raw DSP-ADPCM file in `../SOUND` to WAV (stereo assumed split in file halves). `RenderModelTemplate`, `Animation`, `Clank*`, `.files` manifests and `.mo` movies are not decoded |
| **Atomic Planet PUB** | Wii / Assets | ✅ | ❌ | *AMF Bowling: Pinbusters!* `*WII.PUB`: big-endian nested section headers, objects from 0x40 plus a trailing table of 28-byte `{hash, offset, class, size}` entries; class 1 textures carry a palette offset/count, mip count, GX format and `{offset, size, height, width}` per mip level (height first); C8 (IA8 / RGB565 / RGB5A3 palettes), RGBA8 and I8 to PNG. Class 8 meshes: a root at +0x74 lists per-part display lists (GX strips / lists / quads with u16 indices per attribute), texture name hashes and attribute slot records (position f32x3, colour RGBA8, UV s16x2 / 1024, normal s16x3 / 16384); exported as GLB with the diffuse PNG by hash. `effects`, `strap` and `SFX` packages use a different header and are left as-is. |
| **Humongous Resource.rez** | Wii / Archive | ✅ | ❌ | *Backyard Football '10* `Resource.rez`: big-endian 24-byte slots `{offset, size, unpacked size, type, flags}`; a footer sector gives the slot count and the slot table sits before it. Group slots (flags = table size) end in a resource table; resources are byte-LZ compressed (flag bit 0; u32 size, then `0x80|n` literal runs and `n, u16 distance` back-references, from `Decompress` in `Release.elf`). Type 73 textures (width, height, GX format 5/6/8/9, RGB5A3 palettes) to PNG; type 76 sounds (`{6, rate, 0, data bytes}`, 16 coefficients at +0x44, DSP-ADPCM frames from +0x80) to WAV; slots that are not groups (548 more textures, 26 sounds, 6 `THP` videos) are handled too. Group ids are slot index + the footer's first slot (50). Type 75 models are the game's depth-first object tree (224-byte header with rest transform and pivot, GX display list, position / colour / normal / UV arrays, batches naming their texture by `group << 16 | index`, children after 32-byte padding); vertices are in world space. Type 86 is the matching animation tree (40-byte keyframes: position, quaternion, scale) exported as glTF translation / rotation / scale channels at 30 fps relative to each object's rest pose about its pivot. Skinned models (55 resources: player parts on a 29-bone skeleton) export with a skin in bind pose: per position 3 bone indices + weights, bone tree of local offsets. Type 7 motions (61 frames x 29 bones: root offset f32x3 + s16 quaternions / 32767 per frame, stride 0xf4) become skeleton-only GLBs using the joints of the first skinned model, root offset relative to frame 0, 30 fps. Types 24, 44, 46, 52 are not decoded. `release.rcf` / `release.dsf` on the disc are signed network-update blobs, not game data. |
| **BombShell data pack** | Wii, PC / Archive | ✅ | ❌ | *Bee Movie Game* `.xwi` (Wii, big-endian, 32-byte aligned) and `.xdx9` (PC, little-endian): one memory image `0x020100a0, 0x040100af, 0x138, n` with 24-byte directories `{-, type (1 interface, 3 link, 4 world), -, -, size, datapack offset}`; each datapack has a 192 (Wii) / 188 (PC) byte header (+4 sound bytes, +8 sound table words, +20 textures, +32 sounds, +108 word list). Sounds are `{data offset, size, rate, flags}` records whose data is a whole FSB3 bank (Wii) or RIFF WAVE (PC) with its source path right behind it. Textures are 32-byte records in an offset list (format byte 0x45 / 0xc5 / 0xc6 = CMPR on Wii where 0xc5 / 0xc6 keep alpha as a second CMPR image; 0x45 DXT1, 0xca DXT5, 0xa0 BGRA8, 0x18 BGR8 on PC; log2 width / height; pixel, aux, name and alpha offsets). Base level only. Layout from the Pac-Kart project. Wii models are found through the datapack's model patch list (`+56` entries `{u32 pointer, u16, u16}` at `datapack + u32(+0) + m`, where `m` is the patch list offset minus `u32(+0)`; all pointers add `m`): a model record `{u16, u16, u32 name pointer (Content\\shapes\\...), +16 list count, +20 list of {count, pointer array}}` whose sub-mesh records (224 bytes; type 0 static, 1 skinned-part, 2 and 3 not decoded) point at raw buffers: `+0xb0` vertex flags (bit 0 position, 1 normal, 2 colour, 3 UV: one big-endian u16 index each per vertex, in that order), `+180` float positions, `+188` (type 0) / `+184` (type 1) colours, `+192` float normals, `+196` s16 8.8 UVs and `+200` `{0, byte count, pointer}` to a GX display list `{command (0x90 triangles, 0x98 strip, 0xa0 fan, 0x80 quads), BE u16 count, vertices}`. A sub-mesh's texture is the patch list entry (`datapack + u32(+0) ...`: texture entries `{u32 pointer, u16, u16}` start at `m - 8 * (u32(+16) + u32(+52) + u32(+28) + u32(+84))`) whose pointer equals the field at sub-mesh `+48` (or `+56`); its second u16 is the texture index within the directory. Exported as one GLB per model in `DIR/models` (LOD 0, positions, normals, UV0, the PNG embedded per sub-mesh; no skeleton; degenerate strip joins dropped, some stray triangles remain). Not decoded: skinned weights and skeletons, types 2 / 3, PC models and world data. |
| **Blue Tongue TRB** | Wii / Archive | ✅ | ❌ | *de Blob 2* `.trb` (`TRB\0`, version 0x7d1, big-endian; not the TSFB layout of Barnyard): 0x30-byte section descriptors at 0x80 `{0, name offset, 0, flags, size, size, file offset}` (section 0 is the string table; `.data`, `__s00000` pixel pool, `gpu_data`, `model_collision`, `XUR`), then 16-byte symbols `{tag, offset, section << 16, name offset}`. A `ttex` symbol is a texture object: `0x0df00bb0` fill, then GX format (0 I4, 1 I8, 2 IA4, 3 IA8, 5 RGB5A3, 6 RGBA8, 8 C4, 9 C8, 14 CMPR), pixel offset in the pool at +0xc, palette offset +0x14 and format +0x18, stored size +0x1c, width / height pairs at +0x40 (+0x44 in UI files; first pair is the stored size) and the `.tga` name; only the base level is decoded. The `XUR` section is written as a `.xur` file. `tcmd` symbols are models: +0x10 batch count, +0x14 offset of a batch offset array; a 0x68-byte batch record holds the display list (size +0x40, offset +0x44 in `gpu_data`) and `{array offset, attribute << 24 \| element size << 16 \| count}` pairs for positions (s16x3, fraction bits from the batch's format table), normals (s8x3), TEX0 and TEX1 (s16x2); GX triangle / strip / fan / quad primitives with one u8 (u16 above 256 entries) index per attribute. Exported as one GLB per model (positions, normals, UV0; no materials; props hang below Y = 0, so the engine may be Y-down). The format table `{n, n x {b0, b1, fraction bits, attribute}}` (from the record's +4 offset) lists every vertex attribute; entries below 9 are inline index bytes with no array, and the `{array offset, descriptor}` pairs (record +0x14; display list size and offset are the two words before them) exist only for attributes 9 and up (colour arrays are indexed but not exported). A count with bit 15 set means the array is shared: in a `Cell*.trb` the position / normal / TEX1 arrays of a prop are those of the same-named model in the level's `LevelAssets.trb`, while its TEX0 array is the cell's own lightmap UV set (the extractor loads `LevelAssets.trb` from the same directory). Not decoded: props whose model is not in `LevelAssets.trb` (about half of the props of the *Blanc House* level), texture binding (a batch record carries no material: `LevelAssets.trb` holds `GXSET_*_Mem` GX state pools, `CoreMaterials_Mem` and a `CoreMaterialSets_Mem` table of `{model name pointer, count, offset, count x pointer}` entries whose pointers lead into linked property lists in `.data`; the node layout is unknown), collision data and the `CETB` compressed level. |
| **Toshi TSFB (TRB/TTL)** | Wii / Assets | ✅ | ❌ | Blue Tongue *Nickelodeon Barnyard* (layouts after the OpenBarnyard decompilation): `TSFB` big-endian hunks `XRDH` header, `TCES`/`CCES` section (`CCES` = BTEC `CETB` LZ77 with 6/14-bit lengths and 7/15-bit offsets, optional XOR), `CLER` relocations, `BMYS` symbols `{u16 section, u16 name offset, u32 hash, u32 data offset}`. `.ttl` (symbol `TTL`): 13-word texture entries (format `0x300 +` I4..CI8_IA8, name, size, data/palette pointers, mip count) decoded to PNG (first level). `.tkl` (symbol `keylib`, `TKeyframeLibrary::TRBHeader`): quantized bone-animation keyframe pools (packed-s16 translations scaled per-axis, packed-s16 unit quaternions, float scales) decoded to YAML; verified against the OpenBarnyard decompilation and cross-checked on ~250 disc samples, except scale keyframes (no disc sample has any). `.trb` models (`Header`/`Materials`/`Skeleton`/`LOD0_Mesh_n` symbols), `.trz` and `TSFL` little-endian files are not decoded |
| **h.a.n.d. FBC bundle** | Wii / Archive | ✅ | ❌ | *Miffy no Omochabako* `.fbc`: big-endian `00 14 00 06`, `u32 n`, six table offsets, then `n` member offsets at `0x40` (relative to `0x40`); the last `2n` 32-byte slots of the file hold the member sizes (`u32`, `0`-padded) then NUL-terminated names. Members back to back; nested bundles share the layout. Leaves are `.brres`, `.tpl`, `.brasd`, `.ssb` scripts and `csvbin`/`txtbin` message tables (last two not decoded) |
| **Vblank BFP / BAP** | Wii / Archive | ✅ | ❌ | `gamedata_wii.bfp` (`BFP2`, little-endian): 16-byte `{hash, offset, size, stored}` members plus 256 numbered `{offset, size, stored}` slots; stored raw when `size == stored`, else zlib. Names are `h = (h << 1) ^ T[(h & 255) ^ upper(c)]` hashes (table in `lib-vblank.c`); the ~145 recovered names come from the game DOLs and package contents, others keep `<hash>.<ext>` with content-sniffed extensions. `audio_*_Wii.bap`: `BPP3` (*Shakedown*) sounds hold 1..n streams (kind `0x100` Ogg Vorbis → `.ogg`, kind `0x20` mono DSP-ADPCM with a big-endian DSP header → `.wav`) named after their title metadata; `BAP1` (*Retro City Rampage*) holds zlib-compressed `BTRK` tracker modules plus a raw 8-bit sample pool (`samples.raw`); the tracker/sample binding and the `.rec`/`.map`/`.til` game-data formats are not decoded. Layouts reversed from `main.dol` |
| **FMOD FSB** | Wii / Audio bank | ✅ | ❌ | FSB3 (*Nickelodeon Barnyard*; 0x18-byte header, same sample headers, 4-channel music) and FSB4 (*Up*, *WALL-E*, *Cars 2*, *Birthday Party Bash*; incl. basic-header banks) and FSB5 (*Brave*): DSP-ADPCM (mono / stereo, 2-byte interleave) and PCM16 samples → WAV; other codecs copied out as `.bin`. FSB3 not handled |
| **kRAW music** | Wii / Audio | ✅ | ❌ | *Geometry Wars: Galaxies*: `"kRAW"`, u32 size, then big-endian s16 mono samples from +8 (separate `_Left` / `_Right` files per stereo pair; 32 kHz inferred from the 64.08 s loop lengths, not stored in the file). Written as `audio.wav`. `GameAudioDataWii.spd/.spt` (SPT index of 0x18-byte records into the SPD blob) and `Level/*.bin` are not decoded |
| **Avalanche THB/TBB** | Wii / Texture | ✅ | ❌ | Avalanche Software engine textures (*Cars 2*, inside the game's `.zip` / `.tszip` archives): big-endian `.thb` header (one or many textures: offset table + 32-byte records) + tiled GX `.tbb` pixels (I8 / RGB5A3 / RGBA8 / CMPR) → PNG; atlases write `name_0.png`, `name_1.png`, ... |
| **Blitz REV** | Wii / GameCube / Archive | ✅ | ❌ | Blitz Games "Babel" `.rev` (Wii) and `.gcp` (GameCube) packages (*SpongeBob SquarePants: Creature from the Krusty Krab*, `Packages_Rev/` + `AudioRev/`): CRC-keyed index with names matched by CRC; textures (RGBA8 / RGB5A3 / RGB565 / CI4 / CI8 / CMPR / I4 / I8) → PNG; static and soft-skinned actors → GLB (bind pose, no bones) |
| **Cars GCT / GCG** | GameCube / Wii / Texture + Model | ✅ | ❌ | Rainbow Studios engine (*Disney-Pixar Cars*; assets ship loose on GameCube, inside U8 `.arc` on Wii): `.gct` CMPR / CI8 textures → PNG; `.gcg` indexed-strip geometry → GLB with the texture named by the material's `.gcm`. Multi-object animation `.gcg` files are recognised but not converted |
| **Konami ZMB** | Wii / Model | 🟡 | ⛔ | Konami Bemani engine skinned character model (*Dance Dance Revolution: Winx Club*, `DATA/files/character/CHR*.bin`, big-endian). The file is a concatenation of two independent, identically-shaped `"ZMB GC\0\0"` chunks — a body model and a separate head/hair model meant to attach at the body's `mii_head` bone (DDR lets players mix-and-match faces/hairstyles) — each wrapped in its own outer `"WII\0"` header (u32 unknown=2, then 3 section pointers/sizes: chunk offset, chunk end, trailing-subsection offset+size; the 2nd chunk's own trailing subsection runs to EOF). Each `"ZMB GC"` chunk header (relative offsets from the chunk's own start) points to three fixed-stride tables: a texture-name table (u32 count, then 0x20-byte NUL-padded `.tga` name slots — body: 4 skin/eye-ref textures, head: 12 incl. per-slot `_a` alpha variants and 6 `eye0N` blink frames), a material table (u32 count, ~0x40-byte records referencing texture slots and RGBA colours), and a bone table (u32 count, fixed 0xa0-byte records starting with a name field, at least one 3x4/4x4 float transform mostly-identity in the bind pose, and parent/child index fields). Body bone table: 54 bones, standard biped rig (`Hips` → `Spine`/`Spine1` → `Neck`/`Head`, `Left/RightUpLeg`.../`Left/RightHandThumb2`) plus a `mii_head` attachment bone and `ZDRAW_kira_*`/`AccL*`/`AccR*` cloth-drawing helper bones. Head bone table: hair/face rig (`bone_head`, `DB_side`, `rot_DBbone`, `DB_hair01..04`, `ZDRAW_1..3`). Geometry after the bone table (verified on the small, single-bone `mii_head_acc*.bin` props, e.g. `mii_head_acc08.bin` = a 2-quad face-gem prop): a tightly-packed `f32[3]` position array (no interleaving, no header — its address is only known via a face record's position-index array); faces are quads defined by a small per-face record group `{ u32 (type<<16 | count=4), then `count` u32 absolute file offsets }`, where each of those offsets leads to a `u32[4]` index array — one such group for the position indices into a quad (observed `[5,6,4,7]` and `[1,2,0,3]`, decoding to two mirrored, coplanar quads at ear height — confirms the layout) and a parallel group for a per-corner normal index (into a small `f32[3]` normal array, one normal per flat-shaded face) and what is presumably a colour/UV index (seen as all-zero on this untextured-looking prop). Not yet confirmed on a full skinned `CHR*.bin` mesh (more faces, UVs, bone weights, non-quad topology, and the exact table that lists how many faces/attribute-groups exist are all still open); still no `IsZMB`/decoder in `lib-*`. Repro: extract `DATA/files/accessory/mii_head_acc08.bin` from a *DDR: Winx Club* disc, position array at file offset `0x310` (8 verts), face index arrays at `0x288` and `0x298` |
| **Hyrule Warriors Legends** | 3DS / Archive | ✅ | ✅ | Split `.idx` / `.bin` archive pair |
| **BFSHA** | Wii U / Switch / Shader Archive | ✅ | ❌ | NintendoWare shader archive (FSHA): per-model option choice values, sampler extras, uniform-block type names; embedded `.bnsh` sidecars |
| **BNSH** | Switch / Shader | ✅ | ❌ | NintendoWare binary shader: dual source/binary programs per variation, zlib-compressed stages, per-program MemoryData; stage blobs extract as `.bin` / `.glsl` / `.zlib.bin` sidecars |
| **MPR CMDL / SMDL / WMDL** | Switch / Model | ✅ | ❌ | Retro Studios static CMDL, skinned SMDL, and world WMDL models (*Metroid Prime Remastered*, *DKCTF*) |
| **MPR SKEL** | Switch / Skeleton | ✅ | ❌ | Retro Studios skeletal hierarchy (*Metroid Prime Remastered*, *DKCTF*) |
| **MPR PACK** | Switch / Archive | ✅ | ❌ | Retro Studios asset container (*Metroid Prime Remastered*): LE RFRM PACK v1 + TOCC v3, LZSS members |
| **MPR TXTR** | Switch / Texture | ✅ | ✅ | Retro Studios texture (*Metroid Prime Remastered*): LE RFRM TXTR v47/51, Tegra detile, BC1-7/ASTC; `wimgt ENCODE` writes single-mip RGBA8 with a `.mpr.txtr` destination |
| **MSH (PMsh)** | Wii / Model | ✅ | ✅ | Monster Games collision mesh |
| **MSBF / MSBP / MSBT** | Wii/3DS/Wii U/Switch / Text | ✅ | ✅ | Message Studio Binary Text and Flow |
| **MSR** | 3DS / Archive | 🟡 | ⛔ | Metroid: Samus Returns archive |
| **MVDK** | DS / Compression | ✅ | ✅ | Mario vs. Donkey Kong Deflate/LZSS |
| **NDS / SRL / DSI** | DS / ROM Archive | ✅ | ✅ | Nitro ROM pass-through & unpacking |
| **NANR / NCER / NCGR / NCLR** | DS / 2D Graphics | ✅ | ✅ | Nitro 2D cell, sprites, palettes |
| **NCCARC** | DS / Archive | ✅ | ✅ | WarioWare: Touched! container |
| **NLG DICT** | 3DS / Switch / Archive | ✅ | ❌ | Next Level Games dictionary archive (*Metroid Prime: Federation Force*, *Luigi's Mansion 2 HD*, *Luigi's Mansion 3*, *Mario Strikers: Battle League Football*). Typed chunk pass: LM2/LM3/Federation Force models, textures, skeletons, animations, scripts, NLOC/font/config |
| **FEDMODEL / FEDSKEL** | Multi / Model | ✅ | ❌ | Next Level Games model/skeleton containers (extractor intermediates, versions 1..3) → GLB |
| **FEDTEX** | Multi / Texture | ✅ | ❌ | Next Level Games texture container (extractor intermediate: PICA / Switch RGBA8/BCn/ASTC) → PNG |
| **SANIM** | Wii / Animation | ✅ | ❌ | Mario Strikers skeleton animation stream → text dump |
| **NSBMD / NSBTX** | DS / 3D Graphics | ✅ | ✅ | Nitro 3D models and textures |
| **NUD** | Wii U / 3DS / Model | ✅ | ✅ | Bandai Namco 3D model container (Smash 4 NDP3/NDWU and Pokkén NDWD multi-mesh) |
| **NUMATB** | Switch / Material | ✅ | ❌ | Bandai Namco SSBH material container (Smash Ultimate): text manifest plus MatLab-dialect MaterialLibrary XML |
| **NUANMB** | Switch / Animation | ✅ | ❌ | Bandai Namco SSBH skeletal/material animation (Smash Ultimate): group/node/track structure plus decoded Direct/Constant/Compressed keyframe payloads |
| **NUMSHB** | Switch / Model | ✅ | ❌ | Bandai Namco SSBH 3D mesh model (Smash Ultimate) |
| **TTMODEL** | Multi / Model | ✅ | ❌ | TT Games NTT engine model (*LEGO Star Wars: The Skywalker Saga*, `.model` → GLB) |
| **SHARC / SHARCFB** | Wii U / Switch / Shader Archive | ✅ | ✅ | NintendoWare shader source and binary archive (SHARC v10-12 round-trips; Wii U SHARCFB is compiled from GSH) |
| **VFXB** | Wii U / Switch / Effect Archive | ✅ | ❌ | NintendoWare particle effect binary archive |
| **NUT** | Wii U / 3DS / Texture | ✅ | ❌ | Bandai Namco texture package (Smash 4) |
| **NUTEXB** | Switch / Texture | ✅ | ✅ | Super Smash Bros. Ultimate texture container |
| **PAC** | Wii / Archive | ✅ | ✅ | Super Smash Bros. Brawl archive |
| **PLT0** | Wii / Animation | ✅ | ✅ | NW4R palette animation |
| **PRC** | Switch / Param | ✅ | ❌ | Smash Ultimate "paracobn" parameter binary to ParamXML-dialect XML (bool/sbyte/byte/short/ushort/int/uint/float/hash40/string/list/struct); Smash 4 era variants recognised |
| **PSDK** | Wii / Compression | ✅ | ✅ | Prosonic SDK LZSS container |
| **QuickLZ** | Compression | ✅ | ✅ | QLZ 1.20 and 1.4.0 streams |
| **RARC** | GameCube / Wii / Archive | ✅ | ✅ | Nintendo standard resource archive |
| **RFL_Res.dat** | Wii/3DS/Wii U / Mii Database | ✅ | ✅ | Revolution Face Library resource container |
| **RNC1 / RNC2** | Compression | ✅ | ✅ | ProPack compression |
| **RSO** | Wii / Code Module | ✅ | ❌ | Nintendo relocatable PowerPC module (`gamelogic.rso`, *Skylanders: SuperChargers Racing*): no magic, detected structurally (zero module-list pointers, bounds-checked section table); raw code/data sections extracted, BSS section (zero on-disk offset) skipped; extract-only, relocation/linking out of scope |
| **RSEQ / CSEQ / FSEQ / SSEQ** | Wii/3DS/Wii U/DS / Sequence | ✅ | ✅ | Music sequence MML / MIDI |
| **SDAT** | DS / Sound Archive | ✅ | ✅ | Nitro Sound Archive |
| **SMDH** | 3DS / Metadata | ✅ | ✅ | Application icon & title metadata |
| **SSZL / VCRA** | Wii / Compression & Archive | ✅ | ✅ | Namco Museum Remix container |
| **TEX** | Wii / Texture | ✅ | ✅ | Monster Games GX texture |
| **TMPK** | Wii U / Archive | ✅ | ✅ | *The Legend of Zelda: Twilight Princess HD* flat archive. Verified against the retail `content/Shaders.pack.gz`: a plain gzip stream wraps a 10,402,512-byte TMPK archive that extracts to exactly 1568 non-empty members |
| **WARC** | Wii U / Archive | ✅ | ✅ | Game & Wario flat archive |
| **WUD / WUX** | Wii U / Disc Image | ✅ | ✅ | Wii U disc extraction & compression |
| **XTX** | Switch / Texture | ✅ | ❌ | Nintendo Switch intermediate texture container (`DFvN`/`HBvN`, Tegra block-linear RGBA8/BC1-BC7/ASTC; synthetic fixture, no retail sample) |
| **XMB** | Switch / Metadata | ✅ | ❌ | Smash XMB material/LOD metadata: node/property tables to XMBDec-mapping XML |
| **Yay0 / Yaz0** | Compression | ✅ | ✅ | Nintendo standard LZ77 compression |
